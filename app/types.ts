export type FlowerOnCard = {
  id: number;
  x: number;
  y: number;
};

/**
 * This is the data type that holds all information required to render a card.
 *
 * It includes the flowers with their positions, the text to display, and the greenery image.
 *
 * This is the object that will be encoded and stored in the URL when sharing a card design.
 */
// export type CardDetails = {
//   flowers: FlowerOnCard[];
// };
